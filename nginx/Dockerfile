# Base Image: Nginx Alpine
# Selected for minimal attack surface and small footprint.
FROM nginx:alpine

# Copy Configuration
# Replaces the default Nginx config with our secure proxy config.
COPY nginx.conf /etc/nginx/nginx.conf

# Copy SSL Certificates
# These files are generated by generate_certs.py and are required for SSL termination.
COPY certs/ /etc/nginx/certs/
